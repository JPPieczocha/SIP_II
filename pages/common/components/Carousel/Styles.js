import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    container:{
        marginTop: 10
    },

    titleText: {
        fontSize: 32,
        paddingVertical: 5
    }
});

export default styles;